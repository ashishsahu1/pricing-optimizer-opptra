// Runtime configuration — served as a static file, read before the app boots.
// Edit this value to point the deployed UI at your deployed backend, e.g.
//   window.OPPTRA_API_BASE = 'https://opptra-api.onrender.com/api';
// Leave it as localhost for local development.
window.OPPTRA_API_BASE = 'http://localhost:8000/api';
