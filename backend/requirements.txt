# Pinned to versions with prebuilt wheels for Python 3.14.
# Install with binary wheels only:  pip install --only-binary=:all: -r requirements.txt
fastapi==0.138.0
uvicorn[standard]==0.49.0
pandas==3.0.3
scikit-learn==1.9.0
numpy==2.4.6
openai==2.43.0
pydantic==2.13.4
